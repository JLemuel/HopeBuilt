#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../vite/bin/vite.js" "$@"
else
  exec node  "$basedir/../../../../vite/bin/vite.js" "$@"
fi
# cmd-shim-target=C:/Users/ADMIN/Desktop/projects/frontend-only 6-12-2026 (Jan)/node_modules/.pnpm/@vitejs+plugin-react-swc@4._49ed2b0c863c569e86eed2dee9135940/node_modules/vite/bin/vite.js
